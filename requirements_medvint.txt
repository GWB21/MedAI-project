# MedVInT-TD 전용
# 설치 순서:
#   1. pip install -r requirements_base.txt
#   2. pip install -r requirements_medvint.txt
#   3. python scripts/setup.py --model medvint --hf_token YOUR_TOKEN
#      (레포 클론 + 체크포인트 + PMC-CLIP 자동 다운로드)
transformers==4.40.0
