# HuatuoGPT-Vision-7B 전용
# 설치 순서:
#   1. pip install -r requirements_base.txt
#   2. pip install -r requirements_huatuogpt.txt
#   3. python scripts/setup.py --model huatuogpt --hf_token YOUR_TOKEN
#      (레포 클론 + forward kwargs 패치를 자동 처리)
transformers==4.40.0
